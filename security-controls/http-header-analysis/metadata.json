{
  "id": "http-header-analysis",
  "name": "HTTP and HTTPS Header Analysis",
  "description": "Fetches backend response headers for a given API endpoint and evaluates HTTP/HTTPS security-related headers against best practices to produce a structured security report.",
  "version": "1.0.0",
  "scope": "HTTP response headers only (no body content analysis in this control)",
  "inputs": ["api_endpoint_url", "response_headers", "optional_context"],
  "outputs": ["evaluated_headers", "summary", "vulnerabilities", "recommendations", "score_and_grade"],
  "references": {
    "n8n_workflow": "Working API Secure.json",
    "flow_steps": ["HTTP Request → Format headers → AI Agent (this prompt) → Process results → Report"]
  }
}
